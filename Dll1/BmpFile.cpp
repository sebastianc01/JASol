#include "BmpFile.h"
#include "pch.h"
#include <string>
#include <iostream>
#include <cstdio>
#include <fstream>

void readFile(std::string file) {
	std::cout << file;
	//FILE* f;
	//if (fopen_s(&f, file.c_str(), "r") == false) {
	//	//std::cerr << "The file cannot be opened.\n";
	//	//return;
	//}

}